# 品質改善計画

このドキュメントは、tact-downloader の安全性、信頼性、保守性を改善するための実施候補を優先順位順に整理したものです。

## 優先度1: データ破損とパス逸脱を防ぐ（完了）

### ダウンロードを原子的にする

対象: `tact_downloader/client.py`

通信切断やディスク障害による不完全なファイルの残存を防ぐため、最終保存先へ直接書き込まない方式へ変更しました。

対応:

- [x] 同じディレクトリの一時ファイルへ保存する
- [x] 転送完了後に `os.replace()` で最終ファイルへ移動する
- [x] 失敗時は一時ファイルを削除する
- [x] API にサイズ情報があれば保存サイズを検証する

### 保存先を vault 内に限定する

対象: `tact_downloader/downloader.py`

外部入力を含むパスを検証し、vault外への保存とサニタイズ後の名前衝突を拒否する方式へ変更しました。

対応:

- [x] 年度、学期、授業名をパスセグメントとしてサニタイズする
- [x] `DOWNLOAD_BASE` を vault からの相対パスに限定する
- [x] 最終パスを `resolve()` し、`VAULT_ROOT` 配下であることを検証する
- [x] `..` は無視せず不正入力として拒否する
- [x] リソースおよびサイトのサニタイズ後の名前衝突を検出する

### URL検証を強化する

対象: `tact_downloader/client.py`

URLのスキーム、正規化したホスト名、ポート、リダイレクト先を検証する方式へ変更しました。

対応:

- [x] `https` のみ許可する
- [x] 正規化したホスト名とポートを検証する
- [x] リダイレクト先も許可ドメインか確認する
- [x] API URLに埋め込むサイトIDをエンコードする

### 実装・実環境の検証結果

- [x] 優先度1の回帰テストを追加（8件成功）
- [x] 既存classifierテストを実行（97件成功）
- [x] TACTへのCookie再利用ログインを確認
- [x] 2026年度「認知科学演習」のリソース3件を実際に再取得
- [x] APIのサイズ情報と保存後のファイルサイズが3件すべて一致
- [x] 実ダウンロード後に一時ファイルが残っていないことを確認

## 優先度2: テストを信頼できる状態にする（完了）

classifierテストの比較漏れを修正し、主要な処理経路の回帰テストと品質確認の仕組みを追加しました。

対応前の課題:

- `tests/test_classifier.py:46` の `zip()` が件数差を無視する
- サイトID不一致を `continue` してしまう
- 保存先に使われる `course_name` を比較していない
- HTTP、ダウンロード、パス、CLI、認証、Obsidian連携が未試験

### 既存テストの完全比較

次の項目を比較する:

- 件数
- サイトID
- タイトル
- 年度
- 学期
- 授業名

スナップショット生成側も `course_name` など必要な項目を保存するように変更します。

実施結果:

- [x] 件数、サイトID、タイトル、年度、学期、授業名を完全比較するpytestテストへ変更
- [x] スナップショット生成側へ `course_name` を追加

### 追加したテスト

パス処理:

- [x] `../outside`
- [x] 絶対パス
- [x] 空の名前
- [x] 禁止文字
- [x] サニタイズ後の名前衝突
- [x] vault外へ逸脱しないこと

ダウンロード:

- [x] 通信途中の切断
- [x] 一時ファイルの削除
- [x] 原子的な置換
- [x] `--force`
- [x] 既存ファイル
- [x] dry-runでファイルシステムが変化しないこと

HTTPクライアント:

- [x] 401、404、429、500
- [x] タイムアウト例外の伝播
- [x] 不正JSON
- [x] 外部URLとHTTP URLの拒否

Obsidianスコープ:

- [x] `大学/`
- [x] 年度
- [x] 学期
- [x] 授業
- [x] `TACTリソース/` 以下
- [x] vault外のパス

実施結果:

- [x] パス安全性、原子的ダウンロード、通信切断、一時ファイル、既存ファイル、`--force`、dry-runを追加
- [x] HTTPステータス、タイムアウト例外、不正JSON、URL拒否を追加
- [x] Obsidianの各スコープとvault外パスを追加
- [x] 認証Cookie再利用、期限切れ時のフォールバック、自動ログイン順序を追加
- [x] CLIの`--list`、`--all`、dry-runを追加

### 検証結果

- [x] pytestで37件成功（サブテスト6件を含む）
- [x] coverage付きpytestが成功（branch coverage、全体59%）
- [x] Ruff lintが成功
- [x] Ruff format checkが成功
- [x] GitHub Actionsで同じ品質確認を実行するworkflowを追加

なお、HTTPタイムアウト値の設定、再試行、失敗時の非ゼロ終了コード、Cookie保存の保護、CLIとObsidian連携のダウンロード処理共通化は、それぞれ優先度3以降の対応範囲です。

### 品質ツール

最小構成として以下を導入する:

- `pytest`
- `pytest-cov`
- `Ruff`
- GitHub Actionsによるテスト、lint、format check

実施結果:

- [x] `pytest`、`pytest-cov`、Ruffを開発依存関係へ追加
- [x] `pyproject.toml`へpytest、coverage、Ruff設定を追加
- [x] GitHub Actionsへテスト、coverage、lint、format checkを追加

## 優先度3: ネットワーク障害を適切に扱う（完了）

対象: `tact_downloader/client.py:30`, `tact_downloader/auth.py:54`

HTTPタイムアウトがないため、ネットワーク障害時に処理が停止し続ける可能性があります。

対応:

- [x] 接続タイムアウトと読み取りタイムアウトを設定する
- [x] 429、502、503、504などGETの一時障害だけ限定的に再試行する
- [x] 認証失敗、通信失敗、データ不正を別の例外として扱う
- [x] 成功、スキップ、失敗を別々に集計する
- [x] 失敗時は非ゼロの終了コードを返す

実施結果:

- [x] API GETとCookie有効性確認に接続5秒、読み取り30秒のタイムアウトを追加
- [x] 429、502、503、504のみ最大2回再試行し、404、500、401は再試行しないようにした
- [x] `AuthenticationError`、`NetworkError`、`DataError`を追加し、API応答・通信・認証の失敗を分類
- [x] 通常CLIとObsidian連携で成功、スキップ、失敗を集計
- [x] 部分失敗時に通常CLIとObsidian連携が終了コード1を返すようにした
- [x] 失敗時も後続サイト・リソースの処理を継続するようにした

検証結果:

- [x] pytestで44件成功
- [x] Ruff lintが成功
- [x] Ruff format checkが成功
- [x] coverage付きpytestが成功（branch coverage、全体62%）

## 優先度4: Cookieと認証情報を保護する（完了）

対象: `tact_downloader/auth.py:122`

Cookieを通常のJSONファイルとして保存しています。

対応:

- Cookieファイルを所有者のみ読み書き可能にする
- 一時ファイル経由で原子的に保存する
- TACT認証に必要なCookieだけ保存する
- 壊れたJSONを明示的に処理する
- Cookieの保存場所、削除方法、機密性をREADMEに記載する
- `.env` のパスワードとTOTP secretの管理上の注意を記載する

実施結果:

- [x] Cookieファイルを所有者のみ読み書き可能な`0600`で保存し、既存ファイルの読込時も権限を補正
- [x] 同じディレクトリの`0600`一時ファイルへ保存し、`fsync()`後に`os.replace()`で原子的に置換
- [x] ブラウザのCookie取得時にTACTのベースURLを指定し、TACT認証に必要なCookieだけを保存
- [x] 壊れたJSONや不正なCookie配列を明示的に検出し、ブラウザ認証へフォールバック
- [x] READMEへCookieの保存場所、削除方法、機密性、`.env`の認証情報管理を記載

検証結果:

- [x] Cookie権限、原子的保存、保存失敗時の一時ファイル削除、壊れたJSONのフォールバックをテスト

## 優先度5: ダウンロード処理を共通化する

（完了）

ダウンロード処理は、通常CLIとObsidian連携の2箇所で重複していました。
現在は共通サービスへ集約し、両方の呼び出し元が同じ処理を利用します。

対象リソース取得、保存先計算、既存判定、dry-run、ダウンロード、結果集計を共通サービスへ集約します。CLI側は対象選択と表示を担当します。

対応:

- [x] 通常CLIとObsidian連携で共通のダウンロードサービスを使用する
- [x] 対象リソース取得、保存先検証、既存判定、dry-run、ダウンロードを共通化する
- [x] 成功、スキップ、失敗の集計を共通化する
- [x] 個別サイトまたはリソースの失敗後も後続処理を継続する
- [x] 通常CLIとObsidian連携の表示とスコープ選択を分離する

検証結果:

- [x] pytestで53件成功
- [x] Ruff lintが成功
- [x] Ruff format checkが成功

## 優先度6: Obsidianセットアップを安全にする

対象: `scripts/setup-obsidian.py`

課題:

- `data.json` を全面置換し、既存設定を失う可能性がある
- プラグインをハッシュ検証なしで取得している
- ダウンロード途中のファイルが残る可能性がある
- Pythonパスとフォルダパスの引用が安全でない可能性がある
- JSON更新が原子的でない

対応:

- 対象コマンドだけIDで追加・更新する
- 既存設定を保持する
- 更新前にバックアップする
- 一時ファイルから原子的に置換する
- プラグインファイルのSHA-256を検証する
- 空白や引用符を含むパスをテストする

## 優先度7: 仕様とドキュメントを一致させる

修正対象:

- `--all` は「学期を検出できたサイトのみ」と明記する
- dry-runの副作用方針を明記し、可能なら副作用をなくす
- セットアップ手順に `venv/bin/playwright install chromium` を追加する
- 対応Pythonバージョンを明記する
- Obsidian連携のセットアップ方法を追加する
- Cookieと秘密情報の注意事項を追加する
- 学期検出が対応するタイトル形式を実装と一致させる
- `SITE_TITLE_PATTERNS` を正式に使うか、未使用設定として削除する

## 今後の実施順序

優先度3〜5は完了しています。今後は次の順序で実施します。

1. Obsidianセットアップの設定更新を安全にする
2. READMEと実装の仕様を一致させる

## 改善完了の目安

- [x] 途中失敗したダウンロードが次回実行を妨げない
- [x] vault外へファイルを保存できない
- [x] dry-runでファイルやディレクトリが作成されない
- [x] 一部失敗を呼び出し元が終了コードで検出できる
- [x] classifierの授業名変更をテストが検出できる
- [x] 通常CLIとObsidian連携で同じダウンロード処理が使われる
- [x] CIでテストと静的解析が毎回実行される
